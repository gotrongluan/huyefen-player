import React from 'react';
import styles from './videoReact.module.scss';

const VideoReactPlayer = () => {
    return (
        <div className={styles.videoReactPlayer}>
            <div className={styles.title}>video-react Player</div>
            <div className={styles.main}>
                
            </div>
        </div>
    )
};

export default VideoReactPlayer;